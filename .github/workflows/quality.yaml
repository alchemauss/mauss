name: Quality-CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - "**"

jobs:
  check-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14
      - name: Lint
        run: npm install && npm run lint
